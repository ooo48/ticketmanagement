<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ticketmanagement.mapper.ShowMapper">

    <select id="getShows" resultType="org.example.ticketmanagement.pojo.Shows">
        select * from shows
        <where>
            <if test="city != null and city != ''">
                city = #{city}
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>
</mapper>